var assert = require('assert');
// Tests are hierarchical. Here we define a test suite for our calculator.

Test if process creation and deletion is done properly.

/* describe('Test Flask-Python Process', function () {
	// And then we describe our testcases.
	it('Creates', function (done) {
         var subpy = require('child_process').spawn('python', ['./initflask.py']);
		assert.equalsubpy, 2);
		// Invoke done when the test is complete.
		done();
	});

}); */